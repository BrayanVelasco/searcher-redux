const defaultState = []; //arreglo de resultados

function reducer (state= defaultState, {type, payload}){//accion destructuring
    switch(type){
        


        default:
            return state;
    }
}

export default reducer